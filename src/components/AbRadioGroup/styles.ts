import { AbRadioGroupStyles } from "./types";

const styles: { [name: string]: AbRadioGroupStyles } = {
  large: {
    typography: "h5",
    buttonSize: "medium",
  },
  small: {
    typography: "body1",
    buttonSize: "small",
  },
};

export default styles;
